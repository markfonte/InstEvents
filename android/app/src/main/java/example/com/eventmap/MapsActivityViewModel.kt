package example.com.eventmap

import androidx.lifecycle.ViewModel
import example.com.eventmap.data.MainRepository

class MapsActivityViewModel(private val mainRepository: MainRepository) : ViewModel() {
}